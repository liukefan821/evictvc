# Superseded drafts

`evictvc-article-draft-2026-06.*` is the June 2026 draft, written against the
standard `article` class before the paper was moved to `iacrcc`. It was
previously at `paper/evictvc.tex`.

It is superseded by `../submitted-cic2026_3/` (the version sent for review) and
`../revision/` (the current working copy). It is kept because it records the
state of the work before the class change and before the bibliography was
rebuilt on BibTeX.

Do not edit it. The figure here is byte-identical to the one used in every
later version.
